<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:gravity="center"
    android:background="@drawable/battleship_main"
    android:orientation="vertical"
    tools:context="com.example.battleship.MainActivity$PlaceholderFragment" >

    <Button
        android:id="@+id/game_menu"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="48dp"
        android:gravity="right"
        android:text="Game Menu"
        android:textColor="#ff00"
        android:textSize="27dp" />

		<Button
		    android:id="@+id/exit"
		    android:layout_width="205dp"
		    android:layout_height="wrap_content"
		    android:layout_alignLeft="@+id/game_menu"
		    android:layout_alignRight="@+id/settings"
		    android:layout_below="@+id/settings"
		    android:layout_marginTop="58dp"
		    android:gravity="center"
		    android:text="Exit"
		    android:textColor="#ff00"
		    android:textSize="27dp" />

		<Button
		    android:id="@+id/settings"
		    android:layout_width="wrap_content"
		    android:layout_height="wrap_content"
		    android:layout_alignLeft="@+id/game_menu"
		    android:layout_alignRight="@+id/game_menu"
		    android:layout_below="@+id/game_menu"
		    android:gravity="center"
		    android:text="Settings"
		    android:textColor="#ff00"
		    android:textSize="27dp" />
    
</RelativeLayout>
